# Copy the variable names into your shell or local secret manager.
# Never put a real key in this file if the folder will be shared.
OPENAI_API_KEY=
OPENAI_TEXT_MODEL=gpt-5.6-luna
OPENAI_TRANSCRIBE_MODEL=gpt-transcribe

